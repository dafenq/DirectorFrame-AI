# Contributing

This repository is currently maintained as a private production tool.

Before changing generation, queue, asset archive, or provider behavior, run:

```powershell
npm.cmd test
npm.cmd run pack
```

Do not commit API keys, generated storyboard projects, generated images, release installers, or signing certificates.
