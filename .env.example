# Optional generic auto-update feed used by electron-updater.
SHOT_FACTORY_UPDATE_URL=https://your-update-host.example.com/ai-manga-shot-factory/

# Optional Windows code-signing certificate settings for electron-builder.
# Do not commit real certificate paths, passwords, or secrets.
WIN_CSC_LINK=
WIN_CSC_KEY_PASSWORD=
